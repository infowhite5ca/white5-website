{
  "name": "white5-meta-ads-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "npm run types && tsc --noEmit && npm test && npm run build",
    "deploy": "wrangler deploy --keep-vars",
    "dev": "wrangler dev",
    "test": "vitest run",
    "types": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "0.21.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260825.1",
    "@types/node": "^26.3.0",
    "typescript": "5.9.3",
    "vitest": "3.2.4",
    "wrangler": "4.125.0"
  }
}
